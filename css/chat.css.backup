/* =========================================================
   AMIT AI — chat.css
   Message rows, bubbles, attachments, message actions,
   typing indicator, and rendered-markdown / code-block
   styling (used by markdown.js for streamed replies).
   ========================================================= */

#chat-area{
  flex:1;overflow-y:auto;display:none;flex-direction:column;
  padding-bottom:10px;position:relative;
}

.msg-row{
  display:flex;padding:18px 16px;gap:14px;max-width:820px;margin:0 auto;width:100%;
}

.avatar{
  width:30px;height:30px;border-radius:8px;flex-shrink:0;
  display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;color:#fff;
}
.avatar.user{ background:#6e6e80; }
.avatar.ai{ background:linear-gradient(135deg,#12b591,#0d8f6f); font-family:Georgia,serif; }

.msg-content{ flex:1; min-width:0; }
.msg-role{ font-size:13px; font-weight:600; margin-bottom:4px; }
.msg-text{ font-size:15px; line-height:1.65; word-wrap:break-word; }
.msg-text p{ margin:0 0 10px; }
.msg-text p:last-child{ margin-bottom:0; }

.msg-row.user .msg-text{
  background:var(--bubble-user);padding:10px 14px;border-radius:var(--radius);
  display:inline-block;white-space:pre-wrap;
}

/* ---------- Attachments ---------- */
.msg-attachment{ margin-top:8px; max-width:220px; border-radius:10px; overflow:hidden; border:1px solid var(--border); }
.msg-attachment img{ width:100%; display:block; }

.msg-file-chip{
  display:inline-flex;align-items:center;gap:6px;padding:6px 10px;border:1px solid var(--border);
  border-radius:8px;font-size:12.5px;color:var(--text-soft);margin-top:6px;
}

/* ---------- Message actions (copy / speak / regenerate) ---------- */
.msg-actions{ display:flex; gap:4px; margin-top:8px; }
.msg-actions button{
  border:none;background:transparent;color:var(--text-soft);cursor:pointer;
  padding:5px 7px;border-radius:6px;display:flex;align-items:center;font-size:12px;gap:4px;
}
.msg-actions button:hover{ background:#f0f0f0; color:var(--text); }

/* ---------- Typing / thinking indicator ---------- */
.typing{ display:flex; gap:4px; padding:4px 0; }
.typing span{
  width:6px;height:6px;border-radius:50%;background:#9a9aa5;
  animation:blink 1.2s infinite;
}
.typing span:nth-child(2){ animation-delay:.2s; }
.typing span:nth-child(3){ animation-delay:.4s; }
@keyframes blink{ 0%,80%,100%{ opacity:.25; } 40%{ opacity:1; } }

/* ---------- Markdown rendering (used by markdown.js) ---------- */
.msg-text h1, .msg-text h2, .msg-text h3{ margin:14px 0 8px; line-height:1.3; }
.msg-text h1{ font-size:20px; }
.msg-text h2{ font-size:18px; }
.msg-text h3{ font-size:16px; }
.msg-text ul, .msg-text ol{ margin:8px 0; padding-left:22px; }
.msg-text li{ margin-bottom:4px; }
.msg-text blockquote{
  margin:8px 0;padding:6px 12px;border-left:3px solid var(--border);
  color:var(--text-soft);background:#f7f7f8;border-radius:0 8px 8px 0;
}
.msg-text a{ color:var(--accent-dark); text-decoration:underline; }
.msg-text strong{ font-weight:700; }
.msg-text code.inline-code{
  background:#f0f0f1;padding:2px 6px;border-radius:5px;font-size:13px;
  font-family:"SFMono-Regular",Consolas,"Liberation Mono",Menlo,monospace;
}

/* ---------- Code block (syntax highlight container) ---------- */
.code-block{
  margin:10px 0;border-radius:10px;overflow:hidden;border:1px solid #2a2a2a;background:#1e1e1e;
}
.code-block-header{
  display:flex;align-items:center;justify-content:space-between;
  padding:7px 12px;background:#2a2a2a;color:#c9c9c9;font-size:12px;font-family:monospace;
}
.code-block-header .copy-code-btn{
  border:none;background:transparent;color:#c9c9c9;cursor:pointer;font-size:12px;
  display:flex;align-items:center;gap:4px;padding:3px 6px;border-radius:5px;
}
.code-block-header .copy-code-btn:hover{ background:rgba(255,255,255,.08); color:#fff; }
.code-block pre{
  margin:0;padding:14px;overflow-x:auto;font-size:13px;line-height:1.55;
  font-family:"SFMono-Regular",Consolas,"Liberation Mono",Menlo,monospace;color:#e6e6e6;
}
.code-block code{ background:transparent; padding:0; }

/* basic token colors for syntax highlight spans (used by markdown.js highlighter) */
.tok-keyword{ color:#c586c0; }
.tok-string{ color:#ce9178; }
.tok-comment{ color:#6a9955; font-style:italic; }
.tok-number{ color:#b5cea8; }
.tok-function{ color:#dcdcaa; }
.tok-tag{ color:#569cd6; }